/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Main.java to edit this template
 */
package lab2exercises;
/*Import Scanner Class which can be used to scan and process strings of text
import java.util.* //.* imports all classes om the util pacakge while .Scanner imports only the Scanner class
;

*/

/**
 *
 * @author seyib
 */
public class Lab2Exercises {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        byte b = 5;
        short a = 107; 
        int sum = a + b;
        int diff = a-b;
        int div = a/b;
        int mult= a *b;
        int mod = a % b; 
      
        System.out.println("Sum = " + sum + " Difference = " + diff + " Division = " + div + " Multiplication = " + mult + " Remainder = " + mod);
        
        
        /*Using Scanner for Keyboard Input 
        
        Scanner sc = new Scanner(System.in); //creating a scanner object that reads text input        
        String name= sc.next(); //apply a method to extract the String as text
        //System.out.println("Hello " + name);
        
        */
        // TODO code application logic here
    }
    
}

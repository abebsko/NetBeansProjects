/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package lab2exercises;

/**
 *
 * @author seyib
 */
public class Lab2Exercises8 {
    public static void main(String[] args) {
    /* Task 1 
    int maximum = Integer.MAX_VALUE;
       int minimum = Integer.MIN_VALUE;
       int minTest = minimum - 1;
       int maxTest = maximum + 1;
        System.out.println("Largest Int is  "+ maximum + " The smallest Int is " + minimum);
       System.out.println("minTest = " + minTest + " maxTest = " + maxTest);
    
    */
       
    
    /* Task 2 
    double maximum = Double.MAX_VALUE;
       double minimum = Double.MIN_VALUE;
       double minTest = minimum - 1;
       double maxTest = maximum + 1;
        System.out.println("Largest double is  "+ maximum + " The smallest double is " + minimum);
       System.out.println("minTest = " + minTest + " maxTest = " + maxTest);  
     
    */
    
    /*
    int can hold the value since the value assigned is larger
    typecast a whole number to a long by adding capital L at the end 
     long gdp = 1550000000000L;
     System.out.println(gdp);
    */
    
    /* Exercise 2
    int tel = 015; //java compiles any integer prefixed with 0 as an octal literal i.e numbers represented in powers of 8 i.e 0-7
     int telTest = 15;
     System.out.println(tel + " " + telTest);
    best datatype to use instead is String
    */
    
    /* Exercise 4
    int x1, x2, x3, x4 ;
    int y = 31, z = 28 ;
    x1 = y + z / 7 ;
    x2 = (y + z) / 7 ;
    x3 = y + z % 7 ;
    x4 = (y + z) % 7 ;
    System.out.println("x1 = " + x1 + "\n x2 = " + x2);
    System.out.println("x3 = " + x3 + "\n x4 = " + x4); 
        */
    
    /*Exercise 5 
    int a = 2, b= 3, x = 5, y = 10;
      int formula = ((x * y) + 2) - (3 * ((a * b) - 4)); 
      int formula2= (x * y + 2) - (3 * (a * b - 4)); 
      System.out.println(formula + "\n" + formula2);
    */
    
    // TODO code application logic here
    }
}
